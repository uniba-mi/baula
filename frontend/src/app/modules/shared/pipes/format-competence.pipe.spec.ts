import { FormatCompetencePipe } from './format-competence.pipe';

describe('FormatCompetencePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCompetencePipe();
    expect(pipe).toBeTruthy();
  });
});
