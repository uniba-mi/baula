import { ShortenCourseTypePipe } from './shorten-course-type.pipe';

describe('ShortenCourseTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenCourseTypePipe();
    expect(pipe).toBeTruthy();
  });
});
