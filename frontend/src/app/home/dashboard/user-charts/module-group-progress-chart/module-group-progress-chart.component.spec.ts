import { ComponentFixture, TestBed } from '@angular/core/testing';

/* import { ModuleGroupProgressComponent } from './module-group-progress-chart.component';

describe('ModuleGroupProgressComponent', () => {
  let component: ModuleGroupProgressComponent;
  let fixture: ComponentFixture<ModuleGroupProgressComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ModuleGroupProgressComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ModuleGroupProgressComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
