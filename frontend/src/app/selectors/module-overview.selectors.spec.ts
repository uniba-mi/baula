import * as fromModuleOverview from '../reducers/module-overview.reducer';
/* import { selectModuleOverviewState } from './module-overview.selectors';

describe('ModuleOverview Selectors', () => {
  it('should select the feature state', () => {
    const result = selectModuleOverviewState({
      [fromModuleOverview.moduleOverviewFeatureKey]: {}
    });

    expect(result).toEqual({});
  });
}); */
