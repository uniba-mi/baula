import { CourseLangPipe } from './course-lang.pipe';

describe('CourseLangPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseLangPipe();
    expect(pipe).toBeTruthy();
  });
});
