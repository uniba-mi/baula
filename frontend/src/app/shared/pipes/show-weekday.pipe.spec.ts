import { ShowWeekdayPipe } from './show-weekday.pipe';

describe('ShowWeekdayPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowWeekdayPipe();
    expect(pipe).toBeTruthy();
  });
});
