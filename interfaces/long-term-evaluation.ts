export interface LongTermEvaluation {
    personalCode: string,
    evaluationCode: string,
    spName: string,
    semester: number,
    pu: number[],
    peou: number[],
    bi: number,
    use: string,
    nps: number,
    feedback: string
}